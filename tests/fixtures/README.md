# Test fixtures

Only synthetic, non-sensitive fixtures belong in this directory. Never add real
FASTQ content, read identifiers, patient identifiers, credentials, private keys,
tokens, or raw environment dumps.
