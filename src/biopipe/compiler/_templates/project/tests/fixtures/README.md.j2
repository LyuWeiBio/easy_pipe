# Synthetic FASTQ fixtures

These tiny records are deliberately artificial and use reserved `SYNTHETIC_`
identifiers. They contain no patient, donor, instrument, accession, or real
biological sequence data. The validator enforces their size, structure,
pairing, identifier prefix, and fixture-root confinement before execution.
